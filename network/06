# 6. Get the complete path of the file that contains the IP address of the DNS server
# you are using [DEDUCTION]

echo "/etc/resolv.conf"

# This command gives you actual DNS servers' list:
# cat /etc/resolv.conf | awk ' /nameserver/{ print $2 } '
