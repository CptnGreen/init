# 2. Identify and display the Ethernet interface characteristics:
# (a) Identify broadcast address
# (b) Identify all IP adresses which are part of the same subnet

sudo ifconfig en0 | awk '/broadcast/{ print $6 }'
