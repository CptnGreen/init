# 1. In what file can you find the installed version of your Debian? [DEDUCTION]

# File name:
echo "/etc/debian_version"

# Get actual version number:
# cat /etc/debian_version

# More commands for information:
#
# cat /etc/issue
# cat /etc/os-release
# hostnamectl
